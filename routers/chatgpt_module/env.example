# ==========================================
# ChatGPT + Whisper модуль - Настройки
# ==========================================

# ===== CHATGPT НАСТРОЙКИ =====
# Обязательно - ваш OpenAI API ключ
OPENAI_API_KEY=sk-ваш_настоящий_ключ

# Модель ChatGPT (см. актуальный список на https://platform.openai.com/docs/models)
# Reasoning модели (o1, o3, o4 серии) используют max_completion_tokens и не поддерживают temperature
# Обычные модели (gpt-3.5-turbo, gpt-4o) используют max_tokens и поддерживают temperature
OPENAI_MODEL=o4-mini

# Максимальное количество токенов в ответе для обычных моделей
# Для gpt-3.5-turbo, gpt-4o: рекомендуется 1000-4000
OPENAI_MAX_TOKENS=1000

# Максимальное количество токенов для reasoning-моделей (o1/o3/o4 серии)
# Для o1-preview, o3, o4: рекомендуется 5000-25000 (они используют больше токенов)
# Если не указано, будет использовано max(OPENAI_MAX_TOKENS, 5000)
OPENAI_MAX_COMPLETION_TOKENS=5000

# Креативность ответов (0.0-2.0)
# Примечание: reasoning-модели (o1/o3/o4) не поддерживают этот параметр (игнорируется)
OPENAI_TEMPERATURE=0.7

# ===== VISION API НАСТРОЙКИ =====
# ⚠️ ВНИМАНИЕ: Vision API может быть дорогим!
# Включить поддержку изображений (true/false)
# При включении бот сможет анализировать изображения через ChatGPT Vision
VISION_ENABLED=true

# Качество обработки изображений:
# - "low" = низкое качество, дешевле (~85 токенов на изображение + детали)
# - "high" = высокое качество, дороже (~765 токенов базово + детали по размеру)
# Рекомендуется "low" для экономии средств
VISION_QUALITY=low

# Максимальный размер изображения (в МБ)
# Большие изображения автоматически сжимаются для экономии
MAX_IMAGE_SIZE_MB=10

# Качество сжатия JPEG (1-100, где 100 - без сжатия)
# Меньше значение = меньше размер файла = дешевле обработка
# Рекомендуется 85 для баланса качества и стоимости
IMAGE_QUALITY=85

# Максимальное разрешение изображения (пиксели)
# Большие изображения масштабируются до этого размера
# Влияет на количество токенов и стоимость
MAX_IMAGE_RESOLUTION=1024

# Предупреждать о высокой стоимости (true/false)
# При true показывает предупреждения о возможных затратах
VISION_COST_WARNINGS=true

# ===== WHISPER НАСТРОЙКИ =====
# Выберите способ распознавания речи:
# - "api" = OpenAI Whisper API (платно, быстро, точно)
# - "local" = Локальный Whisper (бесплатно, медленно, требует ресурсов)
WHISPER_MODE=api

# Настройки для OpenAI Whisper API (если WHISPER_MODE=api)
# Использует тот же OPENAI_API_KEY что и ChatGPT
WHISPER_MODEL=whisper-1

# Настройки для локального Whisper (если WHISPER_MODE=local)
# Модели: tiny, base, small, medium, large-v3
# tiny - быстрая, но менее точная
# large-v3 - самая точная, но медленная
LOCAL_WHISPER_MODEL=base

# Язык для распознавания (опционально)
# Если не указан или "auto", Whisper попытается определить автоматически
# Примеры: ru, en, de, fr, es, it, ja, ko, zh
# Для автоопределения оставьте пустым или укажите "auto"
WHISPER_LANGUAGE=auto

# ===== АУДИО НАСТРОЙКИ =====
# Максимальный размер аудио файла (в МБ)
MAX_AUDIO_SIZE_MB=25

# Максимальная длительность аудио (в секундах)
MAX_AUDIO_DURATION_SEC=300

# Временная папка для аудио файлов (создается автоматически)
AUDIO_TEMP_DIR=temp_audio

# Автоудаление временных файлов (true/false)
AUTO_CLEANUP_TEMP_FILES=true

# ==========================================
# 💡 ПРИМЕРЫ КОНФИГУРАЦИЙ
# ==========================================

# 🎯 Рекомендуемый - o4-mini (reasoning + экономично):
# OPENAI_MODEL=o4-mini
# OPENAI_MAX_COMPLETION_TOKENS=5000

# 🚀 Для быстрых ответов и экономии:
# OPENAI_MODEL=gpt-3.5-turbo
# OPENAI_MAX_TOKENS=1000

# 🧠 Для сложных задач:
# OPENAI_MODEL=gpt-4o
# OPENAI_MAX_TOKENS=2000

# 🔬 Для максимального качества:
# OPENAI_MODEL=o4-mini-2025-04-16
# OPENAI_MAX_COMPLETION_TOKENS=15000

# 🖼️ Экономичная конфигурация для Vision:
# OPENAI_MODEL=gpt-4o-mini
# VISION_ENABLED=true
# VISION_QUALITY=low
# MAX_IMAGE_RESOLUTION=512
# IMAGE_QUALITY=75

# 🎨 Качественная конфигурация для Vision:
# OPENAI_MODEL=gpt-4o
# VISION_ENABLED=true
# VISION_QUALITY=high
# MAX_IMAGE_RESOLUTION=1024
# IMAGE_QUALITY=95

# ==========================================
# 💰 ПРИМЕРНАЯ СТОИМОСТЬ (январь 2025)
# ==========================================

# Обычные модели (за 1K токенов):
# gpt-3.5-turbo: ~$0.0015 (input) / ~$0.002 (output)
# gpt-4o: ~$0.0025 (input) / ~$0.01 (output)
# gpt-4o-mini: ~$0.00015 (input) / ~$0.0006 (output)

# Reasoning модели (за 1K токенов):
# o1-preview: ~$0.015 (input) / ~$0.06 (output)
# o1-mini: ~$0.003 (input) / ~$0.012 (output)
# o3-mini: ~$0.0011 (input) / ~$0.0044 (output)
# o4-mini: ~$0.0011 (input) / ~$0.0044 (output)

# Vision API (за изображение):
# ⚠️ ВНИМАНИЕ: Vision может быть дорогим!
# gpt-4o-mini (low): ~$0.00001-0.00008 за изображение
# gpt-4o-mini (high): ~$0.0002-0.001+ за изображение
# gpt-4o (low): ~$0.0002-0.001 за изображение
# gpt-4o (high): ~$0.005-0.02+ за изображение
# gpt-4-vision (high): ~$0.01-0.05+ за изображение

# Примеры стоимости Vision:
# • Маленькое изображение (512x512, low): $0.00001-0.0001
# • Большое изображение (2048x2048, high): $0.005-0.05+
# • 100 изображений в день может стоить $1-50+ в зависимости от настроек

# Whisper API: $0.006 за минуту аудио

# ==========================================
# 🚨 ПРЕДУПРЕЖДЕНИЯ ПО БЕЗОПАСНОСТИ
# ==========================================

# Vision API:
# • Изображения отправляются на серверы OpenAI
# • Не загружайте конфиденциальные документы или личные данные
# • Большие/сложные изображения могут стоить $0.01-0.05+ за штуку
# • Мониторьте расходы на https://platform.openai.com/usage

# Общие рекомендации:
# • Регулярно проверяйте баланс API ключа
# • Установите лимиты расходов в OpenAI Dashboard
# • Начните с экономичных настроек и постепенно увеличивайте при необходимости

# ==========================================
# 📚 ПОЛНАЯ ДОКУМЕНТАЦИЯ
# ==========================================
# Актуальный список моделей и цены: https://platform.openai.com/docs/models
# Документация Whisper: https://platform.openai.com/docs/guides/speech-to-text
# Документация Vision: https://platform.openai.com/docs/guides/vision
# Подробные расчеты стоимости Vision: см. VISION_COSTS.md в папке модуля 