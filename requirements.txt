# Core dependencies
aiogram==3.4.1
python-dotenv==1.0.1
openai==1.95.1

# Audio processing dependencies
aiofiles==23.2.1
aiohttp==3.9.1
ffmpeg-python==0.2.0

# Image processing dependencies
Pillow==10.2.0

# Email module dependencies
google-auth==2.29.0
google-auth-oauthlib==1.2.0

# Optional dependencies for local Whisper (install manually if needed):
# openai-whisper>=20231117
# ffmpeg-python>=0.2.0
# torch>=2.0.0  # For GPU acceleration

# Installation commands:
# For Whisper API only: pip install -r requirements.txt
# For local Whisper: pip install -r requirements.txt openai-whisper ffmpeg-python
